{
  "allowedModelIds": {
    "type": "Array",
    "metadata": {
      "displayName": "Allowed Model IDs",
      "description": "List of approved model IDs that can be deployed (format: azureml://registries/registry-name/models/model-name)"
    },
    "defaultValue": []
  },
  "deniedModelIds": {
    "type": "Array",
    "metadata": {
      "displayName": "Denied Model IDs",
      "description": "List of model IDs that are explicitly blocked from deployment (highest priority - overrides all other settings)"
    },
    "defaultValue": []
  },
  "allowedPublishers": {
    "type": "Array",
    "metadata": {
      "displayName": "Allowed Publishers",
      "description": "List of approved model publishers that can be deployed"
    },
    "defaultValue": []
  },
  "effect": {
    "type": "String",
    "metadata": {
      "displayName": "Policy Effect",
      "description": "The effect determines what happens when the policy rule is evaluated"
    },
    "allowedValues": [
      "Audit",
      "Deny",
      "Disabled"
    ],
    "defaultValue": "Audit"
  }
}
